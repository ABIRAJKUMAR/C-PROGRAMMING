#include<stdio.h>
int manin()
{
char alpha;
printf("Enter a char:");
scanf("%ch",&alpha);
if(alpha==ch)
{
    {
        printf("It is an char");

    }
    else
    {
        printf("It is not a char");
    }
}
return 0;
}
